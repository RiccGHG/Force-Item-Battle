import { world, system } from "@minecraft/server";
/**
 * @typedef {Object} timer
 * @property {number} h
 * @property {number} min
 * @property {number} sec
 */
/**@type {timer} */
const timeObj = {
    h: 0,
    min: 0,
    sec: 0
}